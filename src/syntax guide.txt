-- Comments --
// line comments
/* block comments */

-- Functions --
fn optionalName(x, y) { returnValue }
optionalName(x, y)

-- Declaration --
let id = fn (x) { x } // Names are auto-added to unnamed functions
let y = ...
in { z }

-- Identity --

fn #myFn (x) { x }
// Returns ifTrue if x and y have the same identity.
// Two functions are considered to have the same identity if they refer to the same function definition.
#myFn(x, ifTrue, ifFalse)

-- Builtin functions --
// Throw a runtime error when called
stdThrow(_)

// Interprets `n` as a natural number and logs out its value, then returns it.
stdLogNat(n)

// Many other functions are provided to inspect and modify AST trees.
