def thing
def anotherThing {
  link aaa-bbb-ccc

  // Trailing commas are supported
  is(left=self(a=1, b=2)->result, right=3)->result

  def fn {
    def a
    def b
    def result
  }
}

// `:` to access a sub-scope.
// keys are automatically fetched from the function's scope.
anotherThing:fn(a=thing, b=thing)->result